defmodule JidoWatch do
  @moduledoc """
  Behaviour and Jido plugin that turns a Jido agent into a viewer.

  See:

    * `VISION.md` — the product framing
    * `CLAUDE.md` — implementation mental model and the plugin/agent boundary
    * `TEST_TREES.md` — the behaviour contract
  """
end
